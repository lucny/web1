---
import ArticleFilters from './news/ArticleFilters.astro';

const { categories = [], programs = [], years = [] } = Astro.props;
---

<ArticleFilters categories={categories} programs={programs} years={years} />
